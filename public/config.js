// Platzhalter für die Entwicklung. Im Container wird diese Datei beim Start aus
// der Umgebungsvariable IMMICH_API_KEY neu erzeugt (siehe docker/immichbook-config.sh).
// Leerer Key => die App fragt (nur) nach dem API-Schlüssel.
window.__IMMICHBOOK_CONFIG__ = { apiKey: "" };
